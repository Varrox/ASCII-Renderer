# C++ ASCII Renderer

At the moment the required dependencies are:

1. CMake
2. a C++ compiler.

yeah thats about it.  

To compile it, just run  

```bash
cmake -B bin build
cmake --build bin
```

and if you have problems after editing code just delete bin folder and recompile.  
basic cmake stuff.  